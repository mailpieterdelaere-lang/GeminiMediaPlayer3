#include <dt-bindings/zmk/matrix_transform.h>

/ {
    chosen {
        zmk,kscan = &kscan_0;
        zmk,matrix-transform = &matrix_transform_0;
    };

    kscan_0: kscan_0 {
        compatible = "zmk,kscan-gpio-matrix";
        label = "KSCAN_0";
        
        // Pin numbering remains Arduino (2, 4, 6, 18).
        row-gpios = <&gpio0 2>,
                    <&gpio0 4>,
                    <&gpio0 6>;
                    
        col-gpios = <&gpio0 18>;
        
        row-pull-downs; // Active High Rows require pull-downs
        col-pull-ups;   // Active Low Columns require pull-ups
    };

    matrix_transform_0: matrix_transform_0 {
        compatible = "zmk,matrix-transform";
        rows = <1>;
        columns = <3>;
        
        // CRITICAL FIX: Use the consolidated ZMK_MATRIX_TRANSFORM macro
        // which defines the transform in a single call.
        map = <ZMK_MATRIX_TRANSFORM(
            (0,0), (1,0), (2,0)
        )>;
    };

    &led_strip {
        status = "disabled";
    };
};
