#include <dt-bindings/zmk/matrix_transform.h>
#include <dt-bindings/gpio/gpio.h> 

/* Define the key matrix and pin assignments for the custom nRF52840 board. */

/ {
    chosen {
        zmk,kscan = &kscan_0; 
        zmk,matrix-transform = &matrix_transform_0; 
    };

    kscan_0: kscan_0 {
        compatible = "zmk,kscan-gpio-matrix"; 
        label = "KSCAN_0";
        
        // CRITICAL FIX 1: Define simple flag properties FIRST
        row-pull-downs; 
        col-pull-ups;   
        
        // CRITICAL FIX 2: Define complex array properties SECOND
        // This uses the two-argument syntax, which we suspect the nice_nano_v2 base expects.
        row-gpios = <&gpios 11>, <&gpios 13>, <&gpios 15>;
                     
        col-gpios = <&gpios 26>; 
    };

    matrix_transform_0: matrix_transform_0 {
        compatible = "zmk,matrix-transform";
        rows = <1>;
        columns = <3>;
        // Minimal single-line map definition (ZMK syntax fix confirmed)
        map = <ZMK_MATRIX_TRANSFORM_ROW_COLUMN_MAP(0, 0) ZMK_MATRIX_TRANSFORM_ROW_COLUMN_MAP(1, 0) ZMK_MATRIX_TRANSFORM_ROW_COLUMN_MAP(2, 0)>;
    };
};
