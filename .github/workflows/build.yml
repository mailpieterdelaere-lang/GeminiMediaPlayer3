name: ZMK Build:Direct Configuration (Bypassing Shield System)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:stable
    permissions:
      contents: read
      
    steps:
      - name: ⬇️ Checkout Repository (Your Config)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: ⚙️ West Setup and File Configuration
        id: west_setup
        shell: bash
        run: |
          # --- Configuration Variables ---
          ZMK_REVISION="v0.3"
          ZMK_CLONE_DIR="./app_clone"
          ZMK_ROOT="./zmk_workspace"
          CUSTOM_CONFIG_DIR="./config"
          
          # 1. Initialize and Update West Workspace
          git clone --branch "$ZMK_REVISION" https://github.com/zmkfirmware/zmk.git "$ZMK_CLONE_DIR" || exit 1
          mkdir -p "$ZMK_ROOT"
          west init -m "$ZMK_CLONE_DIR" --mr "$ZMK_REVISION" --mf app/west.yml "$ZMK_ROOT" || exit 1
          
          # Change to the ZMK root directory
          cd "$ZMK_ROOT" || exit 1
          west update || exit 1
          
          # 2. Prepare Application Source and Copy Config
          mkdir -p "app"
          cp -r "$ZMK_CLONE_DIR/app/"* "app" || exit 1
          cp -r "$CUSTOM_CONFIG_DIR"/* "app/"
          
          # 3. Source Zephyr Environment
          . "./zephyr/zephyr-env.sh" || { echo "FATAL: zephyr-env.sh sourcing failed."; exit 1; }
          
        # Set the working directory for the subsequent build step
        working-directory: ${{ github.workspace }}/zmk_workspace

      - name: 5. Perform West Build
        id: build_app
        shell: bash
        run: |
          echo "--- 5. Starting Final West Build ---"
          west build -b nice_nano_v2 app
        # IMPORTANT: Allow workflow to continue to logging step even if this fails
        continue-on-error: true

      # CRITICAL DEBUGGING LOG UPLOAD
      - name: 💾 Upload Devicetree Preprocessor Logs (On Failure)
        uses: actions/upload-artifact@v4
        # FIX: Executes always, but the contents are only available if the build step failed.
        # We must rely on the overall step status, not a shell variable.
        if: ${{ always() }}
        with:
          name: devicetree-debug-logs
          # Use the stable workspace path
          path: ${{ github.workspace }}/zmk_workspace/build/zephyr/zephyr.dts.pre
          retention-days: 1

      - name: ⬆️ Upload Final Artifacts
        uses: actions/upload-artifact@v4
        # Only upload the UF2 file if the build succeeded
        if: ${{ success() }}
        with:
          name: zmk-firmware
          path: ${{ github.workspace }}/zmk_workspace/build/zephyr/zmk.uf2
