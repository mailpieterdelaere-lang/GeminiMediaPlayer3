name: ZMK Build:Direct Configuration (Stable CI)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:stable
    permissions:
      contents: read
      
    steps:
      - name: ⬇️ Checkout Repository (Your Config)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: 🚀 Setup, Build, and Log Collection
        id: build_all
        shell: bash
        # Run everything from the default $GITHUB_WORKSPACE (where the code was checked out)
        run: |
          # --- Configuration Variables ---
          ZMK_REVISION="v0.3"
          BOARD_NAME="nice_nano_v2"
          
          # 1. Initialize West Workspace in the current directory
          echo "--- 1. Setting up West Workspace ---"
          west init -l . --mr "$ZMK_REVISION"
          west update || exit 1
          
          # 2. Copy/Prepare Files
          echo "--- 2. Preparing Application Source and Configuration ---"
          # The app folder is created here and contains the original ZMK application source
          cp -r ./zmk/app/* ./app/
          
          # Copy your custom config files (keymap/overlay) to the app root
          cp -r ./config/* ./app/
          
          # 3. Source Zephyr Environment
          echo "--- 3. Sourcing Zephyr Environment Variables ---"
          . "./zmk/zephyr/zephyr-env.sh" || { echo "FATAL: zephyr-env.sh sourcing failed."; exit 1; }

          # 4. Perform West Build and Capture Status
          echo "--- 4. Starting Final West Build ---"
          # Build from the current directory, targeting the 'app' folder
          west build -b $BOARD_NAME ./app
          
          BUILD_STATUS=$?
          
          # Pass build status to be used by subsequent steps
          echo "BUILD_STATUS=$BUILD_STATUS" >> $GITHUB_OUTPUT
          
          if [ $BUILD_STATUS -ne 0 ]; then
             echo "FATAL: West build failed. Log upload will proceed."
          fi
          
      # 💾 Upload Devicetree Preprocessor Logs (On Failure)
      - name: 💾 Upload Devicetree Preprocessor Logs (On Failure)
        uses: actions/upload-artifact@v4
        if: ${{ steps.build_all.outputs.BUILD_STATUS != '0' }}
        with:
          name: devicetree-debug-logs
          # Path uses the standard './build' directory relative to the current workspace
          path: ${{ github.workspace }}/build/zephyr/zephyr.dts.pre
          retention-days: 1

      # ⬆️ Upload Final Artifacts (Runs on Success)
      - name: ⬆️ Upload Final Artifacts
        uses: actions/upload-artifact@v4
        if: ${{ steps.build_all.outputs.BUILD_STATUS == '0' }}
        with:
          name: zmk-firmware
          # Path uses the standard './build' directory
          path: ${{ github.workspace }}/build/zephyr/zmk.uf2
