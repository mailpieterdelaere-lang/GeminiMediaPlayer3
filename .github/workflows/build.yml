name: Build ZMK firmware

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    # Use the official ZMK build environment image
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:stable

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Initialize ZMK and Update Modules
        run: |
          echo "Initializing ZMK with stable manifest..."
          # CRITICAL FIX: Initialize west with the ZMK main manifest,
          # which ensures Zephyr and ZMK versions are compatible.
          west init -l config -m https://github.com/zmkfirmware/zmk
          
          # 2. Fetch all source code
          west update

      - name: Build Firmware
        run: |
          echo "Building firmware for nice_nano_v2 with mediapad3 shield..."
          
          # The ZEPHYR_BASE path should now be correctly established by the newer ZMK code.
          west build -s zmk/app -b nice_nano_v2 -- \
            -DSHIELD=mediapad3 \
            -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"
          
          echo "Build complete."

      - name: Archive Firmware
        uses: actions/upload-artifact@v4
        with:
          name: nice_nano_v2-mediapad3-firmware
          # The built .uf2 file is located here
          path: build/zephyr/zmk.uf2
