name: ZMK Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    # Use a standard ZMK/Zephyr Docker image for consistent environments
    container:
      image: zmkfirmware/zmk-build-arm:stable
    
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4
        with:
          # Fetch all history for west to work correctly
          fetch-depth: 0 

      - name: 📦 Initialize and Update West Workspace
        # This is where the ZMK/Zephyr dependencies are fetched (including zcbor)
        run: |
          # The ZMK environment usually requires initializing from the top-level repo
          west init -l config
          # Then update to pull all modules (including zmk/zephyr)
          west update

      - name: 🔍 DEBUG:Check Workspace Directory Structure (Crucial Step)
        # Line 33 is likely around here, possibly the 'run:' or the start of the script.
        run: | 
          echo "--- Current Working Directory (CWD) ---"
          pwd
          
          echo "--- Listing Contents of expected zmk/zephyr/ path ---"
          ls -la zmk/zephyr/ || echo "🚨 ERROR: zmk/zephyr/ directory not found or inaccessible."

      # The original failing step would be here, but we'll use the fixed command below

      - name: ⚙️ Build ZMK Firmware (Resolution Applied)
        # This step now uses the correct directory structure found in most ZMK user-configs.
        # This is the resolved step that anticipates the "No such file" failure.
        run: |
          # 1. Source the environment script from the correct, known location.
          #    In ZMK user config repos, the main ZMK code (which holds the Zephyr dependency) 
          #    is typically checked out at the root level, not inside 'config/'.
          #
          # Resolution applied: We source the environment setup script from the standard 
          # ZMK/Zephyr module location (which should be at the workspace root).
          . $GITHUB_WORKSPACE/zmk/zephyr/zephyr-env.sh
          
          # 2. Run the actual build command
          west build -b nice_nano_v2 -- -DSHIELD=crkbd_left
